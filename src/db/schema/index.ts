// TODO: implement feature modules here.
// Each module should export its Drizzle table definitions from
// `src/modules/<feature>/<feature>.schema.ts` (or a dedicated
// `<feature>.model.ts`) and be re-exported from this barrel so
// `drizzle-kit` can discover them.
//
// Example:
//   export * from '@modules/users/users.model';
//   export * from '@modules/journals/journals.model';

export {};
